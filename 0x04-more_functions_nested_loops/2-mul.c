#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first number.
 * @b: second number.
 * Return: result of the multiplication of a and b
 */

int mul(int a, int b)
{
int mul_result;
mul_result = a * b;
return (mul_result);
}
